import 'package:flutter/material.dart';

class TestPage extends StatelessWidget {
  TestPage({Key? key}) : super(key: key);
  List<String> ulkeler = ["Almanya", "Türkiye", "Fransa", "Norveç","İspanya"];
  

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Text("ffsdfdsf"),
    );
  }
}
